<h2>設定班級 <span>分組方式</span></h2>

<%= form_for @group, :url => {:action => "create_group"} do |f| %>
	<% if @group.errors.any? %>
    <div class="error_messages">
      <h3>Form is invalid</h3>
      <ul>
        <% @group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %><br><br>
      </ul>
    </div>
  <% end %>
	
	<p class="inline">選擇班級與課程: &emsp;<%= f.select :school_class_id, SchoolClass.all.map{ |c| [c.name, c.id] }, :prompt => "請選擇" %></p>
	<p class="inline">&emsp;<%= f.select :course_id, Course.all.map{ |c| [c.name, c.id] }, :prompt => "請選擇" %></p>
	<p class="inline">&emsp;<%= f.select :mode, options_for_select([["S型分組", 0], ["隨機分組", 1], ["研究分組", 2]]), :prompt => "請選擇" %><br></p>
	<div class="actions">&emsp;<%= f.submit '確認' %></div>
<% end %>

<br><br>
<div>* 請注意，分組方式一旦設定完成，無法中途改變模式</div><br>
<br><br>

